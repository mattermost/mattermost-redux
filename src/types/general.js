// @flow

export type GeneralState = {
    appState: boolean,
    credentials: Object,
    config: Object,
    dataRetentionPolicy: Object,
    deviceToken: string,
    license: Object,
    serverVersion: string,
    timezones: Array<string>
};
